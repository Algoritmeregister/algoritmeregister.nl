---
---
{
    "algoritmeregisters": [
{% assign regs = site.data.algoritmeregisters | sort: 'title' %}
    {% for reg in regs %}
            {
                {% for attr in reg %}
                "{{ attr[0] }}": "{{ attr[1] }}"{% if forloop.last == false %},{% endif %}
                {% endfor %}
            }{% if forloop.last == false %},{% endif %}
    {% endfor %}
    ]
}